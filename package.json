{
  "name": "folkrecipies",
  "version": "1.0.0",
  "description": "Simple Project using api recipies",
  "default":"html.index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "David Rivera",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "core-js": "^3.25.5",
    "regenerator-runtime": "^0.13.9"
  }
}
